spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password

kakao:
  client:
    id: test
    redirect-url: test
  provider:
    login-url: test
    token-url: test
    user-url: test